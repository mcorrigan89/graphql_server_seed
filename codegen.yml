schema: 
  - src/graphql/*.graphql
  - src/data/**/*.graphql
generates:
  src/graphql/resolver.types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: '@app/context#Context'