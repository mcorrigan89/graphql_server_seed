import { ModelViewTemplate } from '@utils/view.template';
import { Context } from '@app/context';
import { {{ pascalCase model }}Model } from './model';

export class {{ pascalCase model }}View extends ModelViewTemplate<{{ pascalCase model }}Model> {
  constructor(context: Context, {{ camelCase model }}: {{ pascalCase model }}Model) {
    super(context, {{ camelCase model }});
  }

}